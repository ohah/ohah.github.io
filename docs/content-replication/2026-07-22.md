# Daily Write Reminder

## Purpose  
Every 30 minutes (Cron): Create one concise "Content Replication Document" at `./CRON_CRD_WRITE_TASKS`.

---

### Cron Task Template
1. **File**: Keep this file in workspace root (`/Users/yoonhb/Documents/workspace/blog`)
2. Format: ONE CRD per run, no preamble or explanations.
3. Naming Pattern:
   ```
   docs/content-replication/YYYY-MM-DD.md (or ./CRON_CRD_WRITE_TASKS)
```

4) Create a single markdown entry and close with `---`.

---

### Your Tasks
- Check what's been published/committed today (`git log`, blog site, or other sources).
  - Example commands before writing.
1. Identify ONE thing worth replicating for another audience/channel/platform.

| Content Type Examples |
      --|
         Blog post → LinkedIn/X thread summary with link|

---

## Key Rules
- 📝 Just one CRD per run (no "Today I..." intros)
   | No preamble or explanations; start directly |

3) Start clearly titled.
4, End after metadata block.

7 Keep all own words—don't copy verbatim.