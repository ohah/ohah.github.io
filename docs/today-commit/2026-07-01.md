---
title: "July 1, 2026"
date: July 2nd
description: ohah daily activity for June-Jul.
---

# Activity Summary — Jul

## 요약 (AI 작성)

Today’s commit and PR list is extensive across the maru repo. Notable fixes:
- renderer split-divider thickening regression fixed with new config support, plus FontIdentityRegistry improvements preventing glyph misidentification
- IME input routing refined in settings search; keybind validation now uses inline banners instead of closing notices to improve UX feedback.
Running spinner UI overhauled: replaced the old icon-based approach (code review) and moved it into a single source state pattern with idle-answer pollution blocking, then further refactored for stability.

On PRs:
- Several bugfix merges address clipboard rendering offsets in sidebar rename IME candidate windows; running waveform display persists even during workspace renaming.
- Cursor blink-fade smoothness introduced (cursor.blinkFadeMs ABI v95) after previous feature discussion (#1125).
- Code review feedback applied to spinner UI: refined state isolation, eliminated stale comments.

Also:
- PR #1131 open for Claude session fan matching improvements; previously discussed fixes pending CLAUDE_CODE_SESSION_ID alignment are now being tracked there.
## 커밋

### ohah/maru
<!-- 기존 요약 (수정됨)를 아래에 복사해서 채우되, 중복된 문단 줄이기 위해 1~3문장으로 축소 -->
- [fix(renderer): atlas FontId stability between frames — shared registry](https://github.com/ohah/maru)
```text
commit: fix atla...44...
```
<!-- 각 커밋을 한두 문단 정도로 요약해서 넣으면 됨. 위 commit 내용 참고해 -->
- [fix(renderer): split-divider thickness regression + new config](https://github.com/ohah/maru)
```text
commit: fix spli...67...
```
<!-- 실제 링크 제목을 유지하되, 요약은 줄이기. 같으므로 중복 방어 -->
- [fix(app): settings search IME routing + inline keybind validation banners](https://github.com/ohah/maru)
```text
commit: fix app...c8...
```
<!-- 1~3문단으로 커밋 집중도 파악할 것. 여기서는 위 commit 리스트를 바탕 문맥에 맞춰 -->
- [docs(agent-session): clarify running spinner tradeoffs (accepted)](https://github.com/ohah/maru)
```text
commit: docs ...325...
```
<!-- 이 커밋은 요약한 내용 반영하는 중임. 실제 브랜치에 맞게 -->
- [fix(app): refine spinner code review feedback — idle answer pollution blocking, single-source state isolation and stale comment cleanup](https://github.com/ohah/maru)
```text
commit: fix ...d85...
```
<!-- 이 커밋은 요약한 내용 반영하는 중임. 실제 브랜치에 맞게 -->
- [feat(app): replace running spinner with codex-style equalizer waveform](https://github.com/ohah/maru)
```text
commit: feat ...4f2...
```
<!-- 여기까지가 일간 요약으로 충분함. 상세 목록은 실제 commit log 참고 -->

## PR (해당일 생성)

| repo | # | status |
------+----|
[ohah/maru](https://github.com/...): [#1131 open] fix(agent) claude session fan matching — CLAUDE_CODE_SESSION_ID
```text`
PR: ohah m[#...]1123-...
```

<!-- 요약에서 마무리. 필요하다면 2~4개의 핵심 PR을 한 문단으로 남김 -->