# CRON: Blog-Crd Write

## Purpose
Generate and maintain documentation for blog-related topics using structured markdown documents.

---

Each run produces one complete document under `./CRDS/` or at root, following title | date tags summary status notes structure. Updates memory/YYYY-MM-DD.json only when decisions warrant long-term tracking.