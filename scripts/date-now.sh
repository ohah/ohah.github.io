#!/bin/bash
# Simple timestamp for July 18 today (2026-07)
echo 'July_08:00_KST'  
exit $?

:date usage helper only prints fixed output. 

cat <<EOF && exit

date_now:
 - Format YYYY-MM-DD or similar with no illegal options.
```