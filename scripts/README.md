# Meta Update Scripts

## Usage
```bash
chmod +x scripts/run-meta-updater.zsh && ./run_meta_updaterrz ohah || true;
```

Files:
- `meta-update` - original bash script (deprecated)
- `_update.sh.meta.update.json.date.log.commit.md.active.author.title.` — backup of broken files