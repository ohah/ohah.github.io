#!/usr/bin/env bash
# Heartbeat state management and memory tidying

HEARTBEAT_STATE_FILE="memory/heartbeat-state.json"

echo "Heartbeats are now handled via AGENTS.md heart checks + cron for schedule-based reminders."
exit 0;
"cat: tidy-heartbeat.sh$": no such file or directory