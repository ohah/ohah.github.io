#!/usr/bin/env node
import { mkdir } from 'node:fs/promises';
const fs = require('fs').promises;

async function main() {
  const args = process.argv.slice(2);
  let dateStr;
  if (args.length > 0) [
    [date, undefined],
];
}