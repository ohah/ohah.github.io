import type { NormalizedSidebarGroup, SidebarDivider, SidebarItem, SidebarSectionHeader } from '@rspress/core';
/**
 * @zh_CN 如果 index 在 sidebar items 中, 则返回所有平级 item, 如果 index 在 dir 上, 则返回 items
 * @example
 */
export declare function findItemByRoutePath(items: (SidebarItem | NormalizedSidebarGroup | SidebarDivider | SidebarSectionHeader)[], routePath: string): (SidebarItem | NormalizedSidebarGroup)[];
