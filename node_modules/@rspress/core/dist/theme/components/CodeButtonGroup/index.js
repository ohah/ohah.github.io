import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useSite } from "@rspress/core/runtime";
import { IconWrap, IconWrapped, SvgWrapper } from "@theme";
import clsx from "clsx";
import { useRef, useState } from "react";
import "./index.css";
import { CopyCodeButton } from "./CopyCodeButton.js";
const useCodeButtonGroup = (initialWrapCode)=>{
    const { site } = useSite();
    const { defaultWrapCode } = site.markdown;
    const [codeWrap, setCodeWrap] = useState(initialWrapCode ?? defaultWrapCode);
    const copyElementRef = useRef(null);
    const toggleCodeWrap = ()=>{
        setCodeWrap(!codeWrap);
    };
    return {
        copyElementRef,
        codeWrap,
        toggleCodeWrap
    };
};
function CodeButtonGroup({ codeWrap, toggleCodeWrap, copyElementRef, showCodeWrapButton = true, showCopyButton = true }) {
    return /*#__PURE__*/ jsx(Fragment, {
        children: /*#__PURE__*/ jsxs("div", {
            className: "rp-code-button-group",
            children: [
                showCodeWrapButton && /*#__PURE__*/ jsxs("button", {
                    className: clsx('rp-code-button-group__button', 'rp-code-wrap-button', codeWrap && 'rp-code-wrap-button--wrapped'),
                    onClick: ()=>toggleCodeWrap(),
                    title: "Toggle code wrap",
                    children: [
                        /*#__PURE__*/ jsx(SvgWrapper, {
                            icon: IconWrapped,
                            className: "rp-code-button-group__icon rp-code-button-group__icon--wrapped"
                        }),
                        /*#__PURE__*/ jsx(SvgWrapper, {
                            icon: IconWrap,
                            className: "rp-code-button-group__icon rp-code-button-group__icon--wrap"
                        })
                    ]
                }),
                showCopyButton && /*#__PURE__*/ jsx(CopyCodeButton, {
                    codeBlockRef: copyElementRef
                })
            ]
        })
    });
}
export { CodeButtonGroup, useCodeButtonGroup };
