import type { RouteMeta, UserConfig } from '@rspress/shared';
interface Args {
    routes: RouteMeta[];
    htmlTemplate: string;
    head: UserConfig['head'];
    ssrBundlePath: string;
}
declare const exportWorkerGlueFn: ({ routes }: Args) => Promise<string[]>;
export default exportWorkerGlueFn;
