import type { ShikiTransformer } from 'shiki';
export interface ITransformerWrapCodeOptions {
    /**
     * Default value for wrapping code
     * @default false
     */
    defaultWrapCode?: boolean;
}
export declare const SHIKI_TRANSFORMER_ADD_WRAP_CODE = "shiki-transformer:wrap-code";
/**
 * @private compile-time only
 */
export declare function transformerAddWrapCode(options?: ITransformerWrapCodeOptions): ShikiTransformer;
