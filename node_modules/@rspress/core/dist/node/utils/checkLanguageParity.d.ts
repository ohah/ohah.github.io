import type { UserConfig } from '@rspress/shared';
/**
 * Checks the parity of language files across multiple languages.
 */
export declare function checkLanguageParity(config: UserConfig): Promise<void>;
