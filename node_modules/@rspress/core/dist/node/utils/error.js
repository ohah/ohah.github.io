import { picocolors } from "../../6289.29817fb4.js";
function createError(message, maxStackLines = 5) {
    const error = new Error(message);
    if (error.stack) {
        const lines = error.stack.split('\n');
        if (lines.length > maxStackLines + 1) error.stack = `${lines.slice(0, maxStackLines + 1).map((line)=>picocolors.gray(line)).join('\n')}\n    ${picocolors.gray('... (truncated)')}`;
    }
    return error;
}
export { createError };
