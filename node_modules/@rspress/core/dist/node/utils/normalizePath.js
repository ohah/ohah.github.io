import { node_os } from "../../9348.1beb3e25.js";
import { node_path } from "../../6289.29817fb4.js";
const isWindows = 'win32' === node_os.platform();
function slash(p) {
    return p.replace(/\\/g, '/');
}
function normalizePath(id) {
    return node_path.posix.normalize(isWindows ? slash(id) : id);
}
export { normalizePath, slash };
