import { createHash as external_node_crypto_createHash } from "../../8995.421b464f.js";
function createHash(str) {
    return external_node_crypto_createHash('sha256').update(str).digest('hex').slice(0, 8);
}
export { createHash };
