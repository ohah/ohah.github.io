import { type UserConfig } from '@rspress/shared';
import type { RouteService } from '../../route/RouteService.js';
export declare function emitLlmsTxt(config: UserConfig, routeService: RouteService, emitAsset: (assetName: string, content: string | Buffer) => void, mdContents: Map<string, string>): Promise<void>;
