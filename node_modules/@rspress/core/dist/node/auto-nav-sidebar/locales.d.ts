import type { NavItem, Sidebar } from '@rspress/shared';
export declare function combineWalkResult(walks: {
    nav: NavItem[];
    sidebar: Sidebar;
}[], versions: string[]): {
    nav: Record<string, NavItem[]>;
    sidebar: Sidebar;
};
export declare function processLocales(langs: string[], versions: string[], root: string, extensions: string[], metaFileSet: Set<string>, mdFileSet: Set<string>): Promise<{
    nav: Record<string, NavItem[]>;
    sidebar: Sidebar;
}[]>;
