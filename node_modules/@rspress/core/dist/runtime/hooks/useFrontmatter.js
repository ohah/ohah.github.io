import { usePage } from "./usePage.js";
const useFrontmatter = ()=>{
    const { page } = usePage();
    const { frontmatter = {} } = page;
    return {
        frontmatter
    };
};
export { useFrontmatter };
