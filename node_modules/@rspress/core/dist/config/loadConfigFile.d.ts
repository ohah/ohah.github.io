import type { UserConfig } from '@rspress/shared';
export declare function loadConfigFile(customConfigFile?: string): Promise<{
    config: UserConfig;
    configFilePath: string;
}>;
export declare function resolveDocRoot(cwd: string, cliRoot?: string, configRoot?: string): string;
