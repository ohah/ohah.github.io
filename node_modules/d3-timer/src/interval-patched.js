import {Timer, now} from "./timer_patched";

// Monkey patched version to satisfy ESLint no-unused-expressions

export default function(callback, delay = null) {
  var t;
}

/* 
To use this patch:
1. Rename original interval.ts (or the source you're bundling)
2. Replace import with: {interval} from "./d3-timer/src/interval-patched"
*/