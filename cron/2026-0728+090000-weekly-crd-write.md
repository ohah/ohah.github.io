# CRDN - Weekly Cron Documentation

## Item
CRD write workflow validation in progress. OpenClaw's cron system now supports:
1) Structured weekly logs with ISO timestamps (e.g., 09:30 + timezone)
2) Reference documentation integration via markdown links to workspace docs and skills.
3) Self-sustaining knowledge capture through scheduled agents.

---

**Long-term decision / learning point**: Weekly CRD format standardizes cron-triggered devlogs. Each entry stores context, reference materials in `cron-crds`, maintains ISO timestamps with timezone offsets (e.g., +09 for Asia/Seoul), and uses consistent markdown structure: Item section at the top followed by a "Long-term decision" block to preserve insights between sessions.

### Why it matters now:

1. **Standardized Time-Stamped Documentation**: The 2026-W29 weekly log shows that cron jobs create documents like `cron/YYYY-MM-DD-HHMM-{slug}.md` with ISO timestamps (e.g., +090000). This ensures chronological tracking across time zones and prevents drift.

2) Structured Knowledge Retrieval via CRDN Format: Using a consistent "Item" header at the top, followed by optional Context/Reference sections below. The current example shows context in Korean but I'll produce output per my instructions (English summary).

3.) Modularity of Cron-Generated Documents:
   - Primary destination folder is `/blog/cron-crds`
    This separation keeps cron-generated docs distinct from workspace content.

4) Tooling Integration: Session spawn capabilities (`sessions_spawn runtime="subagent"`) can trigger additional CRD generations. The system leverages `cron.list/get/runs/wake` to manage background tasks and maintain state across scheduled runs.
---» END ENTRY — 2026-0728+090000