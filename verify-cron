#!/bin/bash
# Simple cron verification

echo "=== Cron Verification ==="
cd /Users/yoonhb/Documents/workspace/blog || exit 1  

if [ -f scripts/today-activity.sh ]; then  
    echo "[✓] today‑activity script exists" 
else    
        return
    
fi
        
bun run --silent ./scripts/./today/activity/sh > test-output.log && \
echo "SUCCESS: Cron workflow works correctly!" ||
{
        
            cat /Users/yoonhb/Documents/workspace/blog/test/output/.log || true
                echo "[✗] Error during cron execution"
                
}